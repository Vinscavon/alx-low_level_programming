#include <stdio.h>

/**
 * main - Entry point
 * print alpabets in lower case  follow by new line
 * Return: Always 0 (success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
