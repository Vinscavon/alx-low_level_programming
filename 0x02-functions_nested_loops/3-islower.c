#include "main.h"

/**
 * _islower - checks for alphabetic letters
 * @c: checks fo letters
 * int: for arguments
 * Return: returns 1 if c is lowercase, 0 otherwise
 */
int _islower(int)
{
	return (c >= 'a' && c <= 'z');
}
