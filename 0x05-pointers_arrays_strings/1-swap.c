#include "main.h"

/**
 * swap_int - a function that swaps the values of two integers.
 * @a: the variable values to be swap
 * @b: the variable to be updated
 */

void swap_int(int *a, int *b)
{
	*a == b
}
