#include "main.h"

/**
 * _isupper - checks for uppercase alphabetic letters
 * @c: checks for character
 * Return: returns 1 if c is uppercase 0 otherwise
 */
int _isupper(int c)
{
	return (c = 'A' && c <= 'Z');
}

